using System;

class Program
{
    // I have exceeded requirements by adding an animation of string symbols
    // after the checklist goal is completed
    // the animation has string characters incrementing in front of a congratulatory message
    // to applaud the hard work
    static void Main(string[] args)
    {
        GoalManager goalManager = new GoalManager();
        goalManager.Start();
    }
}
